version: '3.8'

services:

  artemis:
    image: apache/activemq-artemis
    build:
      context: ./
      dockerfile: docker-file/Dockerfile
    container_name: artemis
    hostname: artemis
    restart: always
    ports:
      - "61616:61616"
      - "8161:8161"
#    volumes:
##     "-~/your local host path: exported volumes in docker file"
#      - ~/springboot-tutorial-docker:/opt/broker
#      - ~/springboot-tutorial-docker:/opt/artemis

