spring:
  artemis:
    mode: native
    user: ${ACTIVE_MQ_USER:artemis}
    password: ${ACTIVE_MQ_PASS:artemis}
    broker-url: ${BROKER_URL:tcp://localhost:61616}
  jms:
    pub-sub-domain: true

# defined by user
queue:
  main: mainQueue
  status: statusQueue
